# Sparkl configuration — copy to .env (never commit real keys)
PORT=3000
# AI study buddy "Sparky" — set ONE of these for live tutoring (else demo mode):
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
# Optional model overrides:
# ANTHROPIC_MODEL=claude-sonnet-4-6
# OPENAI_MODEL=gpt-4o-mini
# Persistent data directory (Render disk, etc.):
# DATA_DIR=/var/data
